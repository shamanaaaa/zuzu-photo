# SMTP – fill in the credentials from your hosting provider
SMTP_HOST=mail.yourdomain.sk
SMTP_PORT=587
SMTP_USER=info@yourdomain.sk
SMTP_PASS=your_email_password

# The address the email is sent FROM (must match your SMTP account)
SMTP_FROM=info@yourdomain.sk

# Where submissions are delivered to
CONTACT_TO=zuzukondek@gmail.com

# Express server port
PORT=3000
